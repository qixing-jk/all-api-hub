# モデルリストと価格比較

> すべての中継サイトのモデル詳細を一目で把握。サイトをまたいだ価格比較、最適なグループの自動選択、一括可用性検証をサポートし、最もお得な選択を支援します。

## 機能の概要

- 💰 **リアルタイム価格比較**: 課金倍率、グループ倍率、為替レートに基づいて、異なるサイトの各モデルの実際の単価を自動計算します。
- 🎯 **最適なパスの推奨**: 「すべてのアカウント」ビューでは、現在の比較範囲内の最安値を示します。同じサイト内では、1つのグループだけが最安の場合に限り「最適グループ」を表示します。
- 🔍 **多次元の詳細フィルタリング**: サイトソース、API認証情報、プロバイダー（OpenAI、Anthropicなど）、課金モード、タグによる組み合わせフィルタリングをサポート。
- ✅ **一括可用性検証**: モデルの接続テスト、トークンの互換性チェック、CLIプロキシの可用性検証をサポート。
- 📊 **課金の透明化**: 「トークン課金（Tokens）」と「リクエスト課金（Per Call）」を明確に区別し、利用可能な場合は入力、出力、キャッシュの実際の単価を表示します。

## アクセス方法

1. 拡張機能のポップアップの左下にある **「モデルリスト」** アイコンをクリックします。
2. または、 **設定 → モデル管理** からアクセスします。

## コア操作ガイド

### 1. データソースの選択

ページ上部のセレクターで、異なるデータソースを切り替えることができます：

- **すべてのアカウント**: 追加したすべてのサイトアカウントを集計し、サイト間比較を有効にします。
- **特定のアカウント**: 指定したサイトの完全なモデルカタログを表示します。
- **API認証情報**: 独立したAPI認証情報（サイトアカウント不要）でサポートされているモデルを表示します。

### 2. インテリジェントな価格比較

モデルカードには、モデルの詳細な課金情報が表示されます：

- **実際の単価**: トークン課金のモデルは **100万トークンあたり (USD/CNY)** に換算し、利用可能な場合は入力、出力、キャッシュの価格を分けて表示します。リクエスト課金のモデルは **1回のリクエストあたり** の価格を表示します。
- **グループ価格**: モデルの詳細を展開すると、利用可能なグループと倍率を比較できます。グループを切り替えると、計算された価格も更新されます。
- **価格バッジ**: 「推定」は利用可能な価格表とグループ情報から計算した価格であり、実際の請求はサービス提供元の情報が優先されます。「最安値」は現在の絞り込み範囲かつ同じ課金方式の中で最も安い選択肢を示します。「最適グループ」は1つの利用可能なグループだけが最安の場合に表示され、同額のグループ同士には優劣を付けません。

### 3. グループの切り替えとシミュレーション

サイトが複数のユーザーグループ（例：`default`、`vip`、`svip`）をサポートしている場合、フィルターバーでグループを切り替えて、異なるティアのリアルタイムの価格変化をプレビューできます。

> 💡 **ヒント**: システムは利用可能なグループの最安価格を使って計算します。複数のグループが同額の場合、そのうち1つを「最適グループ」として表示することはありません。

### 4. モデルの検証

モデルカードの右側にある検証アイコンをクリックします：

- **モデルの検証**: 軽量なリクエストを送信して、モデルが現在本当に利用可能かどうかを確認します。
- **CLI互換性の検証**: モデルがストリーミング出力をサポートしているか、コマンドラインツールから正常に呼び出せるかをテストします。
- **一括検証**: ツールバーの **「一括検証」** をクリックすると、現在フィルタリングされているすべてのモデルのテストをキューに入れます。また、チェックボックスを使用して複数のモデルを手動で選択し、選択したセットに対してのみ検証を実行することもできます。

## リストコントロールオプション

| オプション | 説明 |
|------|----------|
| **検索ボックス** | モデルID（例：`gpt-4o`）またはサイト名によるあいまい検索をサポート。 |
| **課金モード** | 「トークン課金」または「リクエスト課金」でモデルをフィルタリング。 |
| **プロバイダーフィルター** | OpenAI、Anthropic、Google、Metaなどのベンダーのモデルを素早く特定。 |
| **並べ替え** | 名前、価格（安い順）、アカウント数による並べ替えをサポート。 |
| **表示設定** | 人民元価格とエンドポイントタイプを表示するかどうかを設定できます。 |

## よくある質問

| 質問 | 解決策 |
|------|----------|
| なぜ一部のモデルの価格が 0 と表示されるのですか？ | そのサイトがこのモデルを無料に設定しているか、サイトが有効な課金データを提供していない可能性があります。その場合、デフォルトのカタログにフォールバックします。 |
| サイト間比較の為替レートはどのように計算されますか？ | 拡張機能には、比較基準を統一するために 1 USD = 7.3 CNY の固定レート（New APIの慣例を参照）が組み込まれています。 |
| 「最適グループ」とはどういう意味ですか？ | モデルが複数のグループ（例：`default` と `vip`）で利用可能な場合、価格が一意に最も安いグループだけにラベルが表示されます。最安価格が同額なら、どのグループも他より優れているとは表示されません。 |
| 検証結果が「不明」と表示されるのはなぜですか？ | APIキーにそのモデルを呼び出す権限があるか、またはサイトが現在レート制限（Rate Limit）にかかっていないか確認してください。 |

## 関連ドキュメント

- [API 認証情報庫](./api-credential-profiles.md): アカウントなしで `Base URL + API Key` を保存する方法。
- [使用状況分析](./usage-analytics.md): これらのモデルによって発生した実際の支出を表示します。
- [サポートされているサイト](./supported-sites.md): 自動価格認識をサポートするアーキテクチャタイプを表示します。
